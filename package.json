{
  "name": "communication-services-chat-function",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "clean": "rimraf dist",
    "prestart": "npm run clean && npm run build",
    "start": "func start",
    "test": "echo \"No tests yet...\""
  },
  "dependencies": {
    "@azure/communication-chat": "1.3.1",
    "@azure/communication-common": "2.0.0",
    "@azure/communication-identity": "1.0.0",
    "@azure/cosmos": "^4.0.0",
    "@azure/openai": "1.0.0-beta.5"
  },
  "devDependencies": {
    "@azure/functions": "^2.0.0",
    "@types/node": "^14.x",
    "rimraf": "^5.0.0",
    "typescript": "^4.0.0"
  }
}
